# 20260202 vs 20260209 비교 분석 보고서

> 20260202 = 현재 `osan/src/pages/badCodeRegister/` 기준

---

## 파일 구조 비교

| 파일 | 20260202 | 20260209 | 상태 |
|------|:--------:|:--------:|------|
| `types/BadCodeRegisterTypes.ts` | O | O | 변경 |
| `hooks/useCreateBadCodRow.ts` | O | O | **동일** |
| `hooks/useCreateGridColumn.ts` | O | O | 변경 |
| `api/badCodeRegisterApi.ts` | O | O | 변경 |
| `api/mocks.ts` | **X** | O | **신규** |
| `BadCodeRegisterView.tsx` | O | O | **대폭 변경** |
| `BadCodeRegisterPage.tsx` | O | O | **대폭 변경** |

---

## 1. Types - `_op` 필드 제거

```diff
 export type BadCodeRegisterRow = {
-  _op?: 'C' | 'U' | 'D' | '';     // 제거됨
   productCategory: string;
   ...
 };
```

CRUD 상태를 Row가 아닌 **useGrid 프레임워크 내부**에서 관리하도록 변경

---

## 2. useCreateGridColumn - `align`, `isRequired` 추가

| 컬럼 | 추가된 속성 |
|------|------------|
| 제품구분 (productCategory) | `align: 'center'`, `isRequired: true` |
| 불량코드구분 (badCodeCategory) | `align: 'left'`, `isRequired: true` |
| 불량코드 (badCode) | `align: 'left'`, `isRequired: true` |
| 불량코드명 (badCodeName) | `align: 'right'` |
| 사용유무 (useStatus) | `align: 'right'` |
| 비고 (remark) | `align: 'center'` |

---

## 3. API - save 함수 실제 HTTP 호출로 전환

```diff
-export const saveBadCodeRegisters = async (payload: BadCodeRegisterRow[]) => {
-  void payload;
-  return Promise.resolve({ result: 'true', message: '' });
-};
+import { apiPost } from '@/api/httpClient';
+export const saveBadCodeRegisters = async (dsName: string, payload: BadCodeRegisterRow[]) =>
+  apiPost<BadCodeRegisterRow>('/badCodeRegisters', dsName, payload);
```

- `dsName` 파라미터 추가, 실제 `apiPost` 호출

---

## 4. mocks.ts (신규) - MSW 기반 Mock

| 엔드포인트 | 용도 |
|-----------|------|
| `GET /api/bff/.../bad-code-register` | 메인 데이터 |
| `POST .../commands/save` | 저장 |
| `GET .../options/cud-type` | CUD 타입 콤보 |
| `GET .../options/product-category` | 제품구분 콤보 |
| `GET .../options/bad-code-category` | 불량코드구분 콤보 |
| `GET .../options/use-status` | 사용유무 (그리드) |
| `GET .../options/use-status-combo` | 사용유무 (검색콤보) |
| `GET .../options/code-type` | 코드타입 콤보 |

---

## 5. View - Props 9개 -> 3개로 대폭 간소화

```diff
 export type BadCodeRegisterViewProps = {
-  t: (key: string) => string;
-  gridHeaderDefinition: GridColumnDef<BadCodeRegisterRow>[];
   searchSection?: React.ReactNode;
-  onSearch: () => void;
-  onCreate: () => void;
-  onDelete: () => void;
-  onCommit: () => void;
-  onExcel: () => void;
+  commonButtonsProps: CommonButtonsProps;    // 버튼 핸들러 + 권한 통합
   onGridInitialized: (grid: FlexGrid) => void;
 };
```

- `columns` prop도 FlexGrid에서 **제거** -> useGrid가 내부에서 컬럼 설정
- `CommonButtons`는 `{...commonButtonsProps}` spread로 전달

---

## 6. Page - 핵심 아키텍처 변경

### 6-1. 신규 추가

| 기능 | 내용 |
|------|------|
| **권한 체크** | `usePageCrudAuth(PAGE_NAME)` -> `can` 속성으로 버튼 활성/비활성 제어 |
| **삭제 검증** | `onValidateRowDelete` - 사용중(Y) 코드 삭제 차단 |
| **무결성 검증** | `onValidateIntegrity` - 불량코드구분+불량코드 중복 체크 |
| **생성 초기값** | `onBeforeCreate` - 현재 행의 badCodeCategory 복사 |

### 6-2. CloseGuard 내부화

```diff
-// 20260202: 수동 관리 (~20줄)
-const closeGuard = useCloseGuardContext();
-const closeGuardTabId = useCloseGuardTabId();
-useCloseGuard({ isDirty, hasChanges });
-const handleSearchWithGuard = useCallback(() => { ... });

+// 20260209: useGrid 옵션 2줄로 대체
+enableCloseGuard: true,
+enableSearchGuard: true,
```

### 6-3. useGrid 옵션 객체 추가 (4번째 인자)

```typescript
useGrid(createBadCodeRow, fetchBadCodeRegisters, saveBadCodeRegisters, {
  columns: BAD_CODE_REGISTER_COLUMNS,    // 컬럼 정의를 useGrid에 위임
  pageName: PAGE_NAME,
  gridName: 'mainGrid',
  onValidateRowDelete,                    // 삭제 전 검증
  onValidateIntegrity: validateIntegrity, // 저장 전 무결성 검증
  onBeforeCreate,                         // 생성 전 초기값
  enableCloseGuard: true,
  enableSearchGuard: true,
});
```

---

## 7. Page - Import 변경 상세

| 구분 | 20260202 | 20260209 |
|------|----------|----------|
| **제거** | `useCloseGuard, useCloseGuardContext, useCloseGuardTabId` from CloseGuardHooks | - |
| **추가** | - | `BadCodeRegisterRow` type import (validateIntegrity 타입 시그니처용) |
| **추가** | - | `usePageCrudAuth` from `@/hooks/usePageCrudAuthCheck` |
| **추가** | - | `CommonButtonResultType, CommonButtonsProps` from commonControlTypes |

---

## 8. Page - useGrid 반환값 변경

```diff
 const {
   handleSearch,
   handleCreate,
   handleDelete,
   handleCommit,
   handleExcel,
   handleGridInitialized: handleGridInitializedBase,
-  isDirty,        // CloseGuard용 - 제거됨
-  hasChanges,     // CloseGuard용 - 제거됨
 } = useGrid(...)
```

- `isDirty`, `hasChanges`는 더 이상 Page에서 사용하지 않음 (useGrid 내부에서 CloseGuard 처리)

---

## 9. Page - handleSearchWithGuard 제거, handleSearchWithFilters 반환값 변경

```diff
-// 20260202: handleSearchWithGuard 함수 존재 (CloseGuard 래핑)
-const handleSearchWithGuard = useCallback(() => {
-  const runSearch = () => { handleSearchWithFilters(); };
-  if (!closeGuard || !closeGuardTabId) { runSearch(); return; }
-  closeGuard.attemptSearch(closeGuardTabId, runSearch);
-}, [...]);
-
-// View에 전달: onSearch={handleSearchWithGuard}

+// 20260209: handleSearchWithGuard 완전 제거
+// handleSearchWithFilters에서 return 추가
 const handleSearchWithFilters = useCallback(() => {
   ...
-  handleSearch(params);      // void
+  return handleSearch(params); // Promise 반환
 }, [...]);
+
+// commonButtonProps.actions.searchCommand = handleSearchWithFilters (직접 전달)
```

---

## 10. View - Import 변경 상세

| 구분 | 20260202 | 20260209 |
|------|----------|----------|
| **제거** | `import type { BadCodeRegisterRow }` from types | - |
| **제거** | `import type { GridColumnDef }` from GridTypes | - |
| **추가** | - | `import type { CommonButtonsProps }` from commonControlTypes |

---

## 11. mocks.ts - 서버/클라이언트 필드명 매핑 관계

| mocks.ts (서버 스키마) | 프론트엔드 모델 | 비고 |
|----------------------|----------------|------|
| `cudType` | (20260202: `_op`) / (20260209: 제거) | CUD 상태 마커 |
| `prdCi` | `productCategory` | 제품구분 |
| `badCi` | `badCodeCategory` | 불량코드구분 |
| `cudCi` | `useStatus` | 사용유무 |
| `badCode` | `badCode` | 동일 |
| `badCodeName` | `badCodeName` | 동일 |
| `remark` | `remark` | 동일 |

서버-클라이언트 간 필드명 변환 레이어가 필요함을 시사

---

## 핵심 결론: 020~060 py 수정 포인트

| 변경 대상 | 수정 내용 |
|----------|----------|
| **040 (Types)** | `_op` 필드 생성 제거 |
| **040 (GridColumn)** | 컬럼별 `align`, `isRequired` 속성 추가 |
| **050 (API)** | save 함수에 `dsName` 파라미터 추가, `apiPost` 호출, `mocks.ts` 생성 |
| **060 (View)** | Props 간소화, `commonButtonsProps` 패턴, FlexGrid에서 `columns` 제거 |
| **060 (Page)** | `usePageCrudAuth` 추가, useGrid 4번째 옵션 객체(columns/pageName/gridName/validate 콜백/Guard 플래그), CloseGuard 수동코드 제거, `CommonButtonsProps` 조립 |
