# 규칙.pdf 반영 현황

규칙.pdf (TOBE FE CONVENTION v0.7)의 각 항목이 020~060 py 파이프라인 코드에 어떻게 반영되어 있는지 정리한 문서입니다.

---

## 1.1 기본 원칙 (Core Principles)

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| 레거시 격리: 기존 시스템 명칭은 코드 주석과 BFF 내부에서만 허용 | 050 | line 134 `// Service: {service}, Transition: {transition}` | 레거시 서비스명/트랜지션을 주석으로만 출력 |
| 축약 금지: Full Name 사용 원칙 (WorkOrder O, WO X) | - | - | **미반영** - 레거시 ID를 PascalCase로 기계적 변환만 수행 (예: D_MMASM_BAD_CODE → DMmasmBadCode). 의미있는 영어명(예: BadCodeRegister)으로의 변환은 사람이 매핑해야 하므로 자동 변환 한계 |
| DTO/VO 책임 분리: DTO는 API 계약, VO는 UI 모델 | - | - | **미반영** - 현재 types.ts의 Row 타입이 API DTO와 UI VO를 겸용. 자동 변환으로는 분리 기준 판단 불가 |

---

## 1.2 API 네이밍 규칙 (Backend Contract Consumption Rule)

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| 업무 도메인 URL: `/domain/{legacyNexacroCommand}/{legacyNexacroFunction}` | 050 | line 135, 156, 177, 197 `fetch('/domain/{service}/{transition}', ...)` | 모든 API 함수에서 `/domain/` prefix 사용 |

### 1.2.1 API 요청/응답 DTO(Type) 네이밍

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| 단수형 + PascalCase + Req/Res 접미사 | - | - | **미반영** - 현재 `{Screen}Row` 타입만 사용. API 전용 Req/Res 타입 미생성. 실제 Backend 연동 시 수동 추가 필요 |

### 1.2.2 쿼리 파라미터 네이밍 (Search Filters)

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| 쿼리 파라미터 camelCase | - | - | 해당없음 - 현재 모든 API가 POST body 방식이므로 쿼리 파라미터 미사용 |

---

## 1.3 React 컴포넌트 구조 및 네이밍

### 1.3.1 계층 분류

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| Page (...Page): 라우트와 1:1 매칭 | 060 | line 380 `page_name = f"{screen_name}Page"` | `{Screen}Page.tsx` 생성 |
| View (...View): 화면 전체 레이아웃 | 060 | line 185 `view_name = f"{screen_name}View"` | `{Screen}View.tsx` 생성 |
| Section (...Section): 기능 단위 UI 블록 | - | - | **미반영** - View 내부의 Section 분리는 수동 리팩토링 영역. 자동 변환으로는 기능 단위 분리 기준 판단 불가 |

### 1.3.2 Modal 네이밍

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| {행위}{대상}Modal (단수형) | - | - | 해당없음 - 현재 파이프라인에서 모달 생성 미포함 |

### 1.3.3 파일 확장자 및 컴포넌트 분리 기준

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| JSX 포함 컴포넌트는 `.tsx` | 060 | line 370 `{view_name}.tsx`, line 580 `{page_name}.tsx` | View, Page 모두 .tsx |
| 순수 로직(Hook/Util)은 `.ts` | 040, 050 | 040 line 169 `useCreate{screen}Row.ts`, 040 line 355 `useCreateGridColumn.ts`, 050 line 97 `{camelName}Api.ts` | Hook, API 모두 .ts |
| 페이지 전용 컴포넌트: `pages/{resource}/components/` | 030 | line 62 `"components"` subdirs | components/ 폴더 생성 (내용물은 수동 작성 영역) |

---

## 1.4 커스텀 Hook 네이밍

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| use{Action}{Entity} 형식 | 040 | line 169 `useCreate{screen_name}Row.ts` | create 행위 + Row 엔티티 |
| Hook은 camelCase | 040 | line 169, 355 | `useCreate...Row.ts`, `useCreateGridColumn.ts` |

---

## 1.5 이벤트 핸들러 및 기타 변수

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| Internal 핸들러: `handle-` 접두사 | 060 | line 449 `handleSearch`, line 457 `handleCommit`, line 476 `handle{ButtonId}` | Page 내부 함수 전체 적용 |
| External Props: `on-` 접두사 | 060 | line 235 `on{ButtonId}`, line 239 `onSearch`, line 240 `onCommit` | View Props 전체 적용 |
| 표준 버튼 Props - 조회: `onSearch` | 060 | line 239 `onSearch: () => void;` (Props 정의), line 271 `onSearch,` (destructure), line 305 `executeCommand: onSearch` (조회 버튼 연결) | |
| 표준 버튼 Props - 저장: `onCommit` | 060 | line 240 `onCommit: () => void;` (Props 정의), line 272 `onCommit,` (destructure), line 307 `executeCommand: onCommit` (저장 버튼 연결) | |
| 표준 버튼 Props - 삭제: `onDelete` | 060 | line 241 `onDelete?: () => void;` (Props 정의, optional), line 273 `onDelete,` (destructure), line 309 `executeCommand: onDelete` (삭제 버튼 연결), line 467 `handleDelete` (Page 핸들러), line 565 `onDelete={handleDelete}` (Page JSX) | '삭제' 버튼이 있는 화면에서만 조건부 생성 |
| Grid initialized 콜백 | 060 | line 244-246 `on{GridId}Initialized?: (grid: FlexGrid) => void` (View Props), line 359 `initialized={on...Initialized}` (View JSX), line 524-536 `handle{GridId}Initialized` (Page 핸들러, 다중행 헤더/행 번호 적용), line 570-574 `on{GridId}Initialized={handler}` (Page JSX) | 다중행 헤더 또는 행 번호 컬럼이 있는 그리드만 실제 핸들러 전달, 나머지는 `undefined` |

---

## 1.6 인증/권한 네이밍 및 라우트 메타

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| ROLE_ 접두사, requiresAuth 등 | - | - | 해당없음 - 인증/권한 로직은 코드 생성 파이프라인 범위 밖 |

---

## 1.7 Zustand 스토어 네이밍

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| use{Entity}Store, kebab-case -store 접미사 | - | - | 해당없음 - 상태 관리 스토어는 코드 생성 파이프라인 범위 밖 |

---

## 1.8 chadcn 사용 규칙

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| shadcn/ui → src/components/ui | - | - | 해당없음 - chadcn 컴포넌트는 수동 설치/관리 영역 |

---

## 1.9 폴더 구조

### 1.9.1 Frontend (React)

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| 도메인 폴더: 복수형(Plural) + kebab-case | 020, 030, 040, 050, 060 | 020 line 29-35 `to_kebab_case()`, line 606; 030 line 24-28 `to_kebab_case()`, line 42; 040 line 31-35, line 362; 050 line 24-28, line 74; 060 line 26-30, line 598 | kebab-case 반영 완료. 복수형은 **미반영** (레거시 ID에서 의미있는 영어 복수형 자동 도출 불가) |
| `types.ts` 페이지 루트에 위치 | 030, 040 | 030 line 73-74 (`types.ts` 파일 생성); 040 line 128 `os.path.join(output_dir, "types.ts")` | |
| `constants.ts` 페이지 루트에 위치 | 030 | line 73-74 (`constants.ts` 파일 생성) | |
| `hooks/` 서브폴더 | 030, 040 | 030 line 59-63 subdirs (`"hooks"` line 60); 040 line 136-137, 215-216 hooks_dir 생성 및 출력 | |
| `api/` 서브폴더 | 030, 050 | 030 line 61 `"api"` subdirs; 050 line 88-89 api_dir 생성 및 출력 | |
| `components/` 서브폴더 | 030 | line 62 `"components"` subdirs | 폴더만 생성, 내용물은 수동 작성 |

### 1.9.2 Component Terminology

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| Shared Component → `@/components/**` 로 import | 040, 060 | 040 line 250 `@/components/ap-wijmo/grid/GridTypes`, 040 line 261 `@/components/ap-wijmo/grid/Util`; 060 line 215 `@/components/ap-wijmo/grid/GridTypes`, 060 line 424 `@/components/ap-wijmo/grid/Util` | @ alias 사용 |
| Page Component → 같은 페이지 내부 상대 경로 | 040, 050, 060 | 040 line 149, 257 `../types`; 050 line 101 `../types`; 060 line 212 `./types`, line 393, 402, 405, 412, 431 모두 상대 경로 | 페이지 내부는 상대 경로 사용 |

### 1.9.3 API 파일명 규칙

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| 공통 API 파일: camelCase + Api 접미사 | 050 | line 97 `api_file_name = f"{camel_name}Api.ts"` | 예: `dMmasmBadCodeApi.ts` |
| 금지: kebab-case 파일명 | 050 | line 75 `camel_name` 별도 변수로 분리 | kebab-case가 아닌 camelCase를 API 파일명에 사용 |

---

## 1.10 Import Path Rules (Dependency & Boundary Rules)

### 1.10.1 기본 원칙

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| Shared → 반드시 `@` alias | 040 | line 250 `from "@/components/ap-wijmo/grid/GridTypes"`, line 261 `from "@/components/ap-wijmo/grid/Util"` | |
| Shared → 반드시 `@` alias | 060 | line 215 `from '@/components/ap-wijmo/grid/GridTypes'`, line 418 `from '@mescius/wijmo.grid'` (FlexGrid type), line 424 `from '@/components/ap-wijmo/grid/Util'` (applyMultiRowHeaders, applyRowIndexColumn) | 다중행 헤더 또는 행 번호 컬럼이 있는 화면에서만 조건부 import |
| Page 내부 → 상대 경로 허용 | 040 | line 149 `from '../types'`, line 257 `from "../types"` | hooks → types 참조 |
| Page 내부 → 상대 경로 허용 | 050 | line 101 `from '../types'` | api → types 참조 |
| Page 내부 → 상대 경로 허용 | 060 | line 212 `from './types'` (View), line 393 `from './{view_name}'`, line 402 `from './api/{api_file_name}'`, line 405 `from './hooks/...'`, line 412 `from './hooks/useCreateGridColumn'`, line 431 `from './types'` (Page) | Page/View → types, api, hooks 참조 |

### 1.10.2 설계 의도

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| import 구문만 보고 코드 책임 범위 파악 | 040, 050, 060 | 위 1.10.1 전체 | `@/` = 공통, `./` or `../` = 페이지 내부로 구분 가능 |

---

## 1.11 주요 ESLint 규칙 (Linter Rules)

### 1.11.1 전역 규칙 (Global Rules)

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| `console.*` 사용 금지 | 050 | (이전 코드에서 save fallback에 있던 `console.log` 제거) | 생성 코드에 console.log 미포함 |
| `console.*` 사용 금지 | 060 | line 480 `// TODO: {btn_text} handler` | 기존 `console.log('{btn_text} clicked')` → TODO 주석으로 변경 |
| `debugger` 사용 금지 | - | - | 생성 코드에 debugger 구문 미포함 (원래부터 없음) |

### 1.11.2 페이지 전용 규칙 (src/pages/**/*.tsx)

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| Side Effect 호출 금지: top-level scope에서 함수 즉시 호출 불가 | 060 | line 488-496 `// useEffect(() => { ... }, []);` | 초기 데이터 로드를 useEffect 내에서만 수행 (현재 주석 처리 상태) |
| Side Effect import 금지: 전역 CSS 등 import 불가 | 060 | - | 생성되는 Page/View에서 CSS import 미포함 |
| `default export` 사용 강제 | 060 | line 434 `export default function {page_name}()`, line 368 `export default {view_name}` | Page는 `export default function`, View는 마지막에 `export default` |

---

## 1.12 컴포넌트 위치/크기 반영

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| Div/Tab 내부 컴포넌트 절대좌표 변환 | 020 | line 450-457 `apply_offset()`, line 460-469 `calc_child_offsets()`, line 472-519 `find_controls_recursive(element, offsets)` | Div/Tab 진입 시 부모 컨테이너의 left/top/right/bottom 오프셋을 누적하여 자식 컴포넌트 좌표를 Form 기준 절대좌표로 변환 |
| View 컨테이너 폼 크기 적용 | 060 | line 185-192 `position: 'relative', width: {form_width}, minHeight: {form_height}` | Form의 width/height를 View 루트 div에 적용 |
| 컴포넌트 절대 위치 스타일 | 060 | line 145-172 `build_style()` | left, top, right, bottom, width, height → CSS `position: 'absolute'` 인라인 스타일로 변환 |

---

## 1.13 Grid 특수 기능

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| 행 번호 컬럼 (expr: binding) | 040, 060, Util.ts | 040 line 308-324 (`expr:` → `binding: '_rowIndex', isRowIndex: true, isReadOnly: true, header: ' '`); 060 line 122-128 `grid_has_row_index()`, line 416-424 import 조건, line 524-536 handler 생성; Util.ts `applyRowIndexColumn()` → `formatItem`으로 행 번호 표시 | XFDL `expr:currow+1` 바인딩을 Wijmo 행 번호 컬럼으로 변환 |
| 다중행 헤더 (head_rowspan) | 040, 060, Util.ts | 040 line 346-348 (`headerRowSpan: N`); 060 line 113-119 `grid_has_multirow_headers()`, line 415-424 import 조건, line 524-536 handler 생성; Util.ts `applyMultiRowHeaders()` → 커스텀 `MergeManager`로 수직 병합 + `formatItem`으로 중앙 정렬 | XFDL `head_rowspan: "2"` → Wijmo FlexGrid 헤더 행 병합 |
| 커스텀 속성 strip | 060 | line 358 `columns.map(({ headerRowSpan: _h, isRowIndex: _r, ...col }) => col)` | Wijmo가 인식하지 못하는 커스텀 속성(headerRowSpan, isRowIndex)을 FlexGrid에 전달하기 전에 제거 |
| 컬럼 size=0 처리 | 040 | line 312, 331 `if col_size is not None:` | `size: 0`도 유효한 값으로 처리 (`width: 0` → 숨김 컬럼). 기존 `if col_size:`는 0을 falsy로 무시하는 버그 |

---

## Appendix A. 임시 BFF (Backend Mock) 운영 정책

| 규칙 | 반영 파일 | 반영 위치 | 비고 |
|------|-----------|-----------|------|
| A.1~A.5 전체 | - | - | 해당없음 - BFF 운영 정책은 코드 생성 파이프라인 범위 밖. server.py (별도 파일)에서 관리 |

---

## 누락 사항

현재 누락 사항 없음.

---

### 자동 변환 한계로 인한 미반영 사항 (수동 작업 필요)

| 규칙 | 사유 |
|------|------|
| 1.1 축약 금지 (Full Name) | 레거시 ID(D_MMASM_BAD_CODE)에서 의미있는 영어명(BadCodeRegister)으로의 변환은 사람이 매핑 테이블을 제공해야 함 |
| 1.1 DTO/VO 책임 분리 | types.ts의 Row 타입이 API DTO와 UI VO를 겸용. 분리 기준은 Backend API 스펙 확정 후 수동 분리 필요 |
| 1.2.1 API DTO Req/Res 접미사 | Backend API 스펙이 확정되면 WorkOrderReq/WorkOrderRes 형태의 타입 수동 추가 필요 |
| 1.3.1 Section 계층 | View 내부의 기능 단위 Section 분리는 화면 분석 후 수동 리팩토링 필요 |
| 1.9 폴더명 복수형(Plural) | 레거시 ID에서 의미있는 영어 복수형(work-orders/) 자동 도출 불가. 이름 매핑 테이블 필요 |

---

## Appendix B. 배포 환경 (Deployment)

### 서버 정보

| 항목 | 값 |
|------|-----|
| 도메인 | `amore.knowlearn.kr` |
| 서버 IP | `175.126.146.186` |
| SSH 접속 | `ssh knowlearn@175.126.146.186` (SSH Key 인증) |
| 프로젝트 경로 | `/home/knowlearn/project` |

### Docker Compose 구성

| 컨테이너 | 역할 | 포트 |
|-----------|------|------|
| `nginx-proxy` | React SPA 서빙 + Flask API 리버스 프록시 | 80, 443 |
| `amore-flask` | Flask Backend API | 5000 (내부) |
| `pg-dev` | PostgreSQL (개발) | 5433 |
| `pg-prod` | PostgreSQL (운영) | 5432 (localhost) |
| `arango-dev` | ArangoDB (개발) | 8530 |
| `arango-prod` | ArangoDB (운영) | 8529 (localhost) |

### nginx 라우팅

| URL 패턴 | 대상 |
|-----------|------|
| `/api/*` | `amore-flask:5000` (Flask API) |
| `/` (그 외 전체) | React SPA (`index.html` fallback) |

### 주요 배포 명령어

```bash
# React 빌드 (로컬)
cd react && npm run build

# 빌드 결과물 서버 전송
scp -r react/dist/* knowlearn@175.126.146.186:/home/knowlearn/project/amore/mes/react/dist/

# 서버에서 컨테이너 재시작
ssh knowlearn@175.126.146.186 "cd /home/knowlearn/project && docker compose up -d"

# 컨테이너 상태 확인
ssh knowlearn@175.126.146.186 "docker ps"

# Flask 로그 확인
ssh knowlearn@175.126.146.186 "docker logs amore-flask --tail 50"

# 컨테이너 강제 재생성 (설정 변경 시)
ssh knowlearn@175.126.146.186 "docker rm -f nginx-proxy amore-flask && cd /home/knowlearn/project && docker compose up -d"
```

### 주요 API 엔드포인트

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/menus` | 메뉴 목록 조회 |
| GET | `/api/files/{id}/content` | 변환된 파일 내용 조회 |
